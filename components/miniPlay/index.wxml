  <!-- 迷你播放器 -->
  <view
    bindtap="navigator2audio"
    wx:if="{{inPlay}}"
    class="miniPlay-container">
    <image
      class="mini_header Rotation"
      src="{{onPlayAudio.header}}">
    </image>
  </view>